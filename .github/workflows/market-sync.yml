name: Market Data Sync (NSE -> Mongo)

on:
  schedule:
    - cron: "0 * * * *" # every hour
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm ci

      - name: Run NSE sync
        env:
          MONGODB_URI: ${{ secrets.MONGODB_URI }}
        run: node scripts/syncNseToMongo.js